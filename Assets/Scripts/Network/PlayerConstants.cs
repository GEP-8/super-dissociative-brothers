namespace Network {
    public enum PlayerAction
    {
        RightMove,
        LeftMove,
        // UpMove,
        // DownMove,
        Jump,
        Crouch
    }

    public enum MergeStrategy {
        Latest
    }
}